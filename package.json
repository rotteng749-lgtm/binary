{
  "name": "vercel-nextpanel",
  "version": "1.1.0",
  "private": true,
  "description": "Kitsune Panel — Multi-role key management panel (Owner / Admin / Reseller) + client auth API for Vercel",
  "main": "api/auth.js",
  "scripts": {
    "start": "node server.js",
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "test": "node test.js"
  },
  "engines": {
    "node": "18"
  }
}
